name: Build MAUI Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:   # allows manual trigger

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Install MAUI workloads
      run: dotnet workload install maui maui-android

    - name: Restore dependencies
      run: dotnet restore MyAccounts.Mobile/MyAccounts.Mobile.vbproj

    - name: Build MAUI Android
      run: dotnet publish MyAccounts.Mobile/MyAccounts.Mobile.vbproj -f net8.0-android -c Release -o ./output

    - name: Upload APK artifact
      uses: actions/upload-artifact@v3
      with:
        name: MyAccounts-APK
        path: ./output/*.apk
